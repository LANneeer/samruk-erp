[project]
name = "utils"
version = "0.1.0"
description = "Base external library implementations"
authors = [
    {name = "Temirlan Ilik",email = "temirlanilik76@gmail.com"}
]
license = {text = "MIT"}
readme = "README.md"
requires-python = "~3.12"
dependencies = [
]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
